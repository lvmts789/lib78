public class PostgresHelper {
    public static void saveUser(String userId, String userName) {
        // Connect to Postgres and save user
    }
    public static String getUser(String userId) {
        // Connect to Postgres and fetch user
        return "";
    }
}